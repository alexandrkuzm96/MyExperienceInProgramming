<Window x:Class="GoodChoise.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoodChoise"
        mc:Ignorable="d"
        Title="Мэнеджер товаров" Height="450" Width="800"
        Loaded="WindowLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <Button Click="ShowListClick"
                Template="{StaticResource ShowList}"/>
            <Button Click="AddProductClick"
                    Template="{StaticResource AddNewProducts}"/>
            <Button Click="DeleteProductClick"
                Template="{StaticResource DeleteProducts}"/>
        </StackPanel>
        <DataGrid Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Name="toyTable"
                  Loaded="ProductTableLoad">
            <DataGrid.Columns>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
